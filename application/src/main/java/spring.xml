<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
	xmlns:aop="http://www.springframework.org/schema/aop" xmlns:jdbc="http://www.springframework.org/schema/jdbc"
	xmlns:tx="http://www.springframework.org/schema/tx"
	xsi:schemaLocation="http://www.springframework.org/schema/jdbc http://www.springframework.org/schema/jdbc/spring-jdbc-4.1.xsd
		http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-4.1.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">


	<!-- aop:aspectj-autoproxy / -->
	<context:annotation-config />
	<context:component-scan base-package="bankingapplication.application">
	</context:component-scan>

	<!-- Spring Driver Manager does not support connection pooling so we can 
		use dbcp -->
	<!-- <bean id="dataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource"> 
		<property name="driverClassName" value="oracle.jdbc.driver.OracleDriver" 
		/> <property name="url" value="jdbc:oracle:thin:@localhost:1521:oracle" /> 
		<property name="username" value="system" /> <property name="password" value="Jiwan251" 
		/> </bean> -->

	<!-- We use dbcp as follows, just by changing the class path nothing else 
		is changed. -->
	<!-- The jar is to be added but no need to make code changes.Also we can 
		add pool size properties as: -->

	<bean id="dataSource" class="org.apache.commons.dbcp2.BasicDataSource">
		<property name="driverClassName" value="oracle.jdbc.driver.OracleDriver" />
		<property name="url" value="jdbc:oracle:thin:@localhost:1521:oracle" />
		<property name="username" value="system" />
		<property name="password" value="Jiwan251" />
		<property name="initialSize" value="2" />
		<property name="maxTotal" value="5" />
		<!-- maxActive property not used by dbcp2, instead it is maxTotal -->
	</bean>

	<!-- For SimpleJdbcDao -->
	<bean id="accountDetailsDaoImpl"
		class="bankingapplication.application.dao.AccountDetailsDaoImpl">
		<property name="dataSource" ref="dataSource" />
	</bean>

	<!-- Service Bean Created here despite having the annotation -->
	<bean id="accountDetailsService"
		class="bankingapplication.application.service.AccountDetailsService" />

</beans>